spring.application.name=LandingBackend
server.port=${PORT:8080}

spring.datasource.url=${DB_URL:jdbc:postgresql://localhost:5432/landing_db}
spring.datasource.username=${DB_USERNAME:postgres}
spring.datasource.password=${DB_PASSWORD:admin}
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.hibernate.ddl-auto=${DDL_AUTO:validate}
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.show-sql=true

spring.datasource.hikari.maximum-pool-size=3
spring.datasource.hikari.minimum-idle=1

application.security.jwt.secret-key=${JWT_SECRET_KEY:wls-super-secreta-clave-maestra-para-produccion-2026-xYz987!@#}
application.security.jwt.expiration=86400000

# Landing page URLs (24h = 86400000 ms)
application.landing.url.base=${FRONTEND_URL:http://localhost:5173}
application.landing.token.expiration=86400000

# Configuración de Correo Gmail (WebLandingSuite)
spring.mail.host=smtp.gmail.com
spring.mail.port=465
spring.mail.username=weblandingsuite@gmail.com
spring.mail.password=fikpvbifduyogyso
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.ssl.enable=true
spring.mail.properties.mail.smtp.starttls.enable=false

python.api.url=${PYTHON_API_URL:https://wlsuite-python-api.onrender.com}